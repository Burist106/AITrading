# Architecture Decisions and Assumptions

## Milestone 3 production authorization (2026-09-22)

- M3-014: Add an explicitly configured, bounded local SQLite replay archive containing the exact immutable cycle and validated normalized risk input in one transaction before its remote write. A local archive failure blocks publishing that calculated cycle. A remote failure may leave a locally archived, unpublished record; do not imply distributed atomicity or remote confirmation. Historical verification recalculates risk and baseline eligibility at the original timestamp, never current readiness or independent source authenticity. No MT5, network, secret/profile discovery, raw account login/server value, or automatic source acquisition is needed for verification. Keep archives outside the checkout, excluded from Git, and disclose that sanitized financial inputs are not encrypted by SQLite itself.
- M3-015: The user explicitly selected both continued development and commit/push for this delivery. Publish only reviewed project source changes on the existing work branch after current checks; no force-push, merge, release tag, deployment, credential setup, or milestone-completion claim is authorized by this selection.

- M3-007: The end-to-end continuation connects a serialized Worker cycle, a versioned SMA/ATR research baseline, deterministic risk/eligibility evidence, immutable Shadow-only database records, and authenticated owner-scoped pages. Shadow records never enter the legacy approval/proposal/command tables. Source prerequisites remain explicit BLOCK reasons; no fixture fallback or automatic credential/deployment setup is introduced.
- M3-008: Use the bounded `sma3-sma5-atr5-v1` features for `sma-atr-shadow-v1`: WAIT for zero volatility or equal moving averages; otherwise research BUY above / SELL below, with an outward tick-rounded two-ATR stop and a three-times-stop target. This is a deterministic uncalibrated research baseline, not a validated profitable strategy. Minimum sample and calibration evidence cannot be inferred from these bars; failed eligibility cannot grant trading permission.
- M3-009: Persist one immutable cycle per account, UTC minute and pipeline/strategy version, with exact semantic replay and conflicting-replay rejection. Independently append conservative quote-observed hypothetical outcomes. Quotes cannot prove all intra-interval price paths; gaps, expiry, restart uncertainty and missing cost evidence must not become invented favorable fills or net PnL.
- M3-010: Hosted authentication, Worker claim issuance, complete historical equity baselines, independently sourced calendar/cost evidence and the unresolved native transaction-time contract remain operational prerequisites. Implement real bounded transports and typed input boundaries without reading local secrets, issuing credentials, manufacturing histories or silently declaring those prerequisites satisfied. Code integration tests use isolated fictional claims/data and are not real-source acceptance evidence.
- M3-011: Persist the validated risk input digest and independent source receipts alongside bounded checks/amounts. Receipt identity, version, coverage and validity are distinct from MT5 provenance. This identifies exact risk inputs without claiming the repository contains a complete external ledger replay archive; supplying and retaining those genuine sources remains a delivery prerequisite.
- M3-012: The read-only production UI uses server-verified Supabase Auth identity, explicit HTTPS origin, access-only HttpOnly/Secure/SameSite-Strict cookies, early Origin/CSRF checks, bounded cancellable request bodies, owner-filtered reads and complete-count validation. No signup, credential issuance, refresh-token storage, protected writes or fixture fallback is introduced. Current health presentation is persisted evidence, not inferred native liveness.
- M3-013: Security scanners must inspect both tracked and untracked non-ignored project sources. Keep ignored environment/profile files unread, retain bounded history and file-size/type checks, deduplicate paths, and reject unsafe links. Exact reviewed synthetic test literals are narrow exceptions, never path-wide exclusions or changes to the broker API allowlist.
- M3-001: The user clarified that the request is for the real usable Demo system, not an isolated experiment. Stop the newly proposed synthetic-only implementation and replace that scope with production Worker components, tested with fakes but accepting validated real observations. Milestone 3 is now authorized and in progress, not complete. Unresolved native timestamp/reconciliation failures still block real-data progression; approval to implement does not turn a failed smoke into a pass.
- M3-002: Fix normalized historical-order completion-time selection and out-of-window reconciliation evidence before building further stages. These defects can be corrected without inferring a native Python timezone contract. Keep the existing market-policy transaction gate closed.
- M3-003: Start with bounded production market normalization/features and a pure risk evaluator. No fixture fallback, broker write, credential, approval/command consumer, or automatic publication is introduced. Each subsequent integration must preserve confirmed source bindings and versioned evidence. See [the implementation status](MILESTONE_3_IMPLEMENTATION.md); strategy, persistence, dashboard, and real-source acceptance are not implied by a unit-test pass.
- M3-004: Keep base adapter, selected market-normalization policy/version and reconciliation version distinct. The native adapter intentionally tags only market observations with the selected time policy. Accept only the exact expected bindings; do not strip policy suffixes or infer a provider/timezone from data. Recompute current LIVE tick age with the adapter half-age threshold and five-second cap, plus any stricter risk policy.
- M3-005: The first risk calculator supports USD broker tick-value valuation with explicit full ledger, cost, news and safety contexts. Use UTC day/Monday week boundaries, broker-point spread/entry tolerances and downward volume-step sizing. Missing context blocks; no fabricated zero cost, empty ledger, clear news, or default healthy state is a production fallback. Calculation PASS remains non-eligible.
- M3-006: Successful history evidence uses inclusive requested bounds and at most PostgreSQL/Python microsecond timestamp precision. Preserve out-of-window metadata only as incomplete evidence. An additive database constraint validates existing rows and fails rather than rewriting append-only evidence.
- DX-002 (2026-09-22): Docker startup again failed on inaccessible runtime sockets. Stop only the task-started Docker app before preserving verified runtime-only directories as same-parent `run.aurum-recovery-20260922` and `docker-secrets-engine.aurum-recovery-20260922` backups. The second directory contained only one zero-byte socket; `FindFirstFileW` metadata identified tag `0x80000023`, the [Microsoft-defined UNIX socket tag](https://learn.microsoft.com/en-us/openspecs/windows_protocols/ms-fscc/c8e77b37-3909-4fe6-a4ea-2b9d423b1ee4). Failed restarts recreated the first socket, so the final attempt also preserved the regenerated runtime as `run.aurum-recovery-20260922-final` and checked both runtime paths together before startup. Docker engine 28.5.1 then responded. No socket payload or credential was read, and this recovery did not delete any database volume or change Docker settings/WSL registration. All three backups were retained. This is not proof of a permanent repair; subsequent database resets still require independently verified isolated fixture-only contents.

## Offline experiment authorization (2026-09-21)

- OF-001: The user authorized an experiment without MT5. Interpret this as a bounded synthetic replay of the existing read-only reconciliation and recovery foundation, not authorization to implement the full Milestone 3 strategy, Risk Engine, proposal, or dashboard pipeline. Keep production code, profile, transaction-time gates, and release statuses unchanged. Use existing centralized test factories, a fixed clock, deterministic identifiers, and in-memory persistence only. No real credentials, native adapter, network, or database is required.
- OF-002: Keep the experiment under Worker tests, outside the production wheel. Explicitly label its output synthetic and non-eligible; simulated Healthy is not real Worker health, a real smoke pass, proof of broker timestamp semantics, or trading performance. Exercise negative cases, repeatability, recovery, and reconstruction of the poller before reporting results. Test guards reject native/profile imports, common socket access, and environment-based MT5 configuration; these guards are not an OS sandbox. See [OFFLINE_EXPERIMENT.md](OFFLINE_EXPERIMENT.md).

## Transaction evidence availability follow-up (2026-09-21)

- TX-006: The operator confirmed there is no existing Demo trading history and authorized documentary research. Record the [official-source evidence matrix](MILESTONE_3_READINESS.md#transaction-time-documentary-evidence-2026-09-21): Python/native functional correspondence and native server-time/inclusive/completion-time selection narrow the hypothesis, but do not explicitly establish Python datetime serialization or every numeric transaction field's timezone. Preserve the transaction gate; do not extrapolate the market offset, repeat empty probes, create trades, send support messages, or start Milestone 3. This research update changes documentation only, not the runtime contract or native-smoke evidence.

- TX-005: A zero matching-symbol result over seven days cannot rule out older evidence. Permit exactly one optional 30-day inventory lookback (`--lookback-days=30`); keep seven days as the default. Reject all other durations, malformed values, missing source confirmation, and the option on market-check or smoke commands before native reads. Keep the same 1,000-row cap, provider/account/specification checks, conservative coverage interval, sanitized output, and unverified/no-eligibility semantics. This is a bounded read-only investigation, not an unlimited history scan or permission to convert transaction times.

- DX-001: Recover Docker without deleting data: the observed startup report identified an inaccessible `dockerInference` runtime socket. Verify the exact non-reparse runtime directory, its two zero-byte socket entries, the absent destination, and the absence of Docker processes before renaming it to `run.aurum-recovery-20260921` under the same Docker parent. Preserve the original entries for recovery. The engine then answered version `28.5.1`; this is a current recovery, not proof of a permanent Windows/socket fix. No database/data volume, settings, WSL registration, or software version is changed by this recovery. Fresh database test resets are permitted only after confirming no pre-existing Aurum test containers or volumes and creating a new isolated fixture stack.

- TX-001: The user authorized the readiness plan, not Milestone 3 implementation or automatic publication. Continue the existing goal without weakening Demo, Shadow, native allowlist, profile confirmation, or freshness gates. Commit/push/merge remain a separate operator decision.
- TX-002: Add a count-only, explicitly selected Demo transaction inventory inside the serialized native adapter. This diagnostic is not exposed through the runtime read port and cannot clear the transaction-time gate. Check the current Demo/account/provider and confirmed canonical symbol specification before and after each read. Export no identities, aliases, tickets, comments, prices, native timestamps, or raw structures.
- TX-003: Use one seven-day history transport envelope from `capture UTC - 7 days` to `capture UTC + 3 hours`, encompassing the UTC-label and documented summer server-label hypotheses. The whole candidate event window must fit the existing conservative coverage interval. This is deliberately not an established event-time query contract; do not persist these bounds as UTC observations or claim the counts prove completeness. Limit each returned collection to 1,000 rows, reject malformed/failed collections, and discard partial results or shutdown failures. No broker operation may be created to manufacture samples.
- TX-004: Report only matching-symbol/other-symbol counts and time-field shape/seconds-milliseconds consistency counts for fields already consumed by the adapter. An empty inventory, a valid integer, or an agreeing time pair cannot prove timezone or endpoint semantics. If existing independently timed samples or an authoritative exact bridge contract remain unavailable, leave readiness unresolved and identify the missing evidence instead of converting the hypothesis into a runtime rule.

## Explicit bounded market-time correction (2026-09-21)

- MT-001: The user authorized implementing the source-specific correction after investigation. Add only the explicit `pepperstone_demo_market_2026_summer_v1` market policy; preserve `utc_epoch_v1` as the default and preserve raw diagnostic output. This is not authorization for Milestone 3, transaction-time assumptions, account switching, or relaxed safety gates. The dated TI-005 entry below records the earlier investigation state, not the current patch status.
- MT-002: Separate the [published Pepperstone GMT+3 summer server rule](https://pepperstone.com/en-gb/about-us/trading-hours), observed local tick/bar labels, and the conflicting [MQL5 UTC description](https://www.mql5.com/en/docs/python_metatrader5/mt5copyratesrange_py). Use an explicitly bounded UTC interval `[2026-03-09, 2026-11-01)` rather than a guessed perpetual offset or inferred DST calendar. Decode market event labels by subtracting three hours and encode market range bounds by adding three hours; validate a bounded position/range roundtrip independently. No offset is learned from the tick whose freshness is under test.
- MT-003: Require process-only source confirmation, fresh bound Demo/account and confirmed usable XAU/USD specification checks before and after market reads, and an additional public-provider rejection guard. No profile schema migration, remembered policy, credential, automatic fingerprint replacement, or persistent environment setting is introduced. The provider string cannot replace independent binding confirmation.
- MT-004: Validate capture/event/query coverage and each candle's full interval; reject inconsistent seconds/milliseconds and malformed scalars while supporting native integral array values without a NumPy runtime import. Retain the five-second delayed threshold, ten-second stale threshold, thirty-second future allowance, and existing candle/query limits. A separately authorized Windows clock correction is neither this mapping nor permission to adjust time to force a pass.
- MT-005: Market evidence does not prove Position, active Order, Order-history, or Deal-history conventions. The optional policy therefore blocks all four transaction methods before native reads, including potentially empty results. `market_check_passed` grants no eligibility, invokes no smoke, and cannot substitute for complete smoke, reconciliation, current-patch checks, review, or Milestone 3 authorization. See [MT5_MARKET_TIME_POLICY.md](MT5_MARKET_TIME_POLICY.md) for the bounded current-patch verification and unresolved gates.
- MT-006: A saved account/specification match proves only those previously confirmed bindings, not the selected provider policy. A missing or unmatched public-provider guard remains a block even if local code checks pass. Do not use a portal session, user-facing application title, or an observed offset to override it; do not migrate the profile or switch accounts automatically. Keep actual local native evidence outside Git.
- MT-007: Diagnose provider rejection using five allowlisted constants only: missing, invalid, empty, exact recognized MetaQuotes public-company name, or unsupported label. Preserve the existing Pepperstone string-acceptance rule and read-only native call set. The market-check CLI exports a category only for the corresponding account-binding failure, never arbitrary error detail or raw company/account/server values. Classification cannot select a policy, confirm a profile, relax a gate, or establish transaction-time semantics.
- MT-008: Correct an evidentiary assumption: earlier local tick/bar observations were not independently provider-classified, so they cannot establish Pepperstone's Python timestamp contract. A provider portal, operator intent and an apparently matching offset do not prove native-source provenance. The optional implementation remains unverified on the intended source until exact-provider and binding checks plus the bounded market roundtrip pass together. A different source requires separate evidence and authorization, not relabeling this policy.

## Independent timestamp investigation (2026-09-14)

- TI-001: The user authorized independent investigation toward resolving the timestamp blocker. A reply to a particular forum topic is not a prerequisite in itself. The required evidence is a reviewable timestamp contract for the exact source, fields, and query windows; the existing Demo, read-only, binding, freshness, and Milestone 3 gates remain unchanged.
- TI-002: Treat native event labels, capture UTC, and display timezone as distinct. An advancing tick and an internally consistent seconds/milliseconds pair do not independently establish its UTC offset or freshness. Never calibrate an offset from the same tick whose freshness is being checked; a delayed stream can otherwise appear fresh.
- TI-003: The official [server-time discussion](https://www.mql5.com/en/book/common/timing/timing_local_server) describes a client-side estimate, while its [DST discussion](https://www.mql5.com/en/book/common/timing/timing_daylight_saving) explicitly warns that inference from quote sessions can be wrong. These are research leads, not an authoritative Python field conversion or permission to add native calls. Community reports are corroboration only.
- TI-004: A possible future resolution need not claim a permanent all-years DST calendar: independently supported, explicitly bounded conversion intervals could be reviewed instead. This is an investigation option, not an implemented or approved runtime policy. Any proposed policy must cover every consumed field and history-query boundary, reject uncovered or ambiguous intervals, and preserve the original age/drift limits. No guessed offset, account change, dependency upgrade, or normalization change is authorized merely by this option.
- TI-005: Keep actual local observations and environment results outside Git. Offline replay and fake-backed checks cannot establish a current Terminal connection, a real smoke pass, or Milestone 3 readiness. No normalization defect has been proven from the evidence reviewed in this investigation, so runtime code remains unchanged.

## Remembered local Demo profile (2026-09-09)

- LP-001: The user explicitly approved remembering the previously confirmed terminal path, broker symbol, account fingerprint, and specification fingerprint across restarts. This supersedes the process-only restriction in D-005 and the earlier local validation instructions only for this narrow encrypted local profile. No password, full login, full server, credential, smoke opt-in, safety limit, health result, or remote binding is persisted.
- LP-002: Use current-user Windows DPAPI with non-interactive protection, fixed public application-domain entropy, System32-only DLL loading, and no machine-wide/plaintext fallback. Store ciphertext under local application data outside Git; reject repository, reparse-point, hardlink, malformed, unsupported-version, and oversized profile inputs. The encrypted envelope also carries an integrity digest and a strict allowlisted schema.
- LP-003: The desktop setup performs two separate manual account/specification confirmations, explicit terminal/symbol selection, and a final fresh bound-account/specification read in a separate connection before saving. Cancellation, mismatch, or native/shutdown failure prevents that save. Reconfiguration additionally requires an explicit replacement confirmation and compare-and-swap against the prior ciphertext revision. A stale setup lock fails closed, not automatically cleared.
- LP-004: Profile-backed commands are separate from the unchanged environment-backed commands. They reject conflicting binding/limit variables rather than merging configuration sources. Every check revalidates current Demo account and usable XAU/USD specification; diagnostic and full smoke continue their existing time/reconciliation gates. An absent/corrupt profile never auto-discovers, auto-confirms, or falls back to another identity.
- LP-005: The GUI is local-only, has no automatic MT5 activity on opening, and has no smoke, broker-write, credential, account-switching, or trading control. Its two confirmation dialogs show only masked identity and allowlisted specification fields locally. No fingerprint copying is required. The separately invoked profile-smoke command still requires the per-process explicit opt-in and never saves it.
- LP-006: DPAPI does not protect against compromise of the same Windows user or an administrator, nor provide rollback-proof history. Python cannot guarantee wiping every immutable memory copy. No user-profile/Docker/Windows security setting is changed. This is a reviewed usability follow-up, not a timestamp correction, real smoke pass, database confirmation mechanism, or Milestone 3 implementation. See `LOCAL_MT5_PROFILE.md` for operation and recovery limits.

## Read-only timestamp diagnostic follow-up (2026-09-08)

- D-001: The operator explicitly authorized a separate-branch timestamp diagnostic after the existing real Demo preflight was blocked. This authorization permits diagnostic source changes without claiming a proven normalization defect; it does not authorize Milestone 3 implementation, execution, or relaxed safety rules.
- D-002: Add an explicit local `worker:mt5:tick-time` command and a native-adapter-only capability outside the polling/persistence port. Account binding and the previously confirmed usable XAU/USD specification are rechecked before one `symbol_info_tick` read. Only its strictly typed numeric `time`/`time_msc` fields and the observation clock cross this diagnostic boundary.
- D-003: Preserve both native fields, including absent/zero millisecond evidence and disagreement between fields. UTC interpretations and signed age are diagnostic hypotheses, not proof of the broker convention. No offset is inferred or applied; the existing latest-tick normalization, 10-second age limit, 30-second future limit, smoke, polling, and reconciliation policies are unchanged.
- D-004: Diagnostic exit zero means only `observed` after successful shutdown. Output explicitly says `grants_eligibility=false` and `smoke_invoked=false`; it never marks market data, Worker health, or the real smoke as passed. Missing bindings block before native startup. The dedicated command itself is the local opt-in; it is not called by startup, polling, CI against a real terminal, or the browser.
- D-005: Machine-specific paths, identities, broker aliases, and confirmation fingerprints remain operator-entered and process-local. They are absent from diagnostic JSON, tracked evidence, and browser/database schemas. This follow-up adds no dependencies, schema change, confirmation action, or broker API.
- D-006: The goal is readiness to begin Milestone 3, not permission to bypass the unresolved real-terminal time gate. See `MILESTONE_3_READINESS.md`; no completion or new real-terminal pass is claimed by these implementation changes.

Bootstrap decisions remain in force unless a later explicitly authorized milestone supersedes them. Milestone 1 decisions add a local database and authorization foundation without authorizing broker integration or execution.

## Recorded decisions

| ID    | Decision                                                                                                                                                                                                                                     | Rationale and consequence                                                                                                                                                                                                                                                                                                                  |
| ----- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| B-001 | Use a pnpm workspace with `apps/web`, `apps/worker`, `packages/contracts`, and `fixtures`.                                                                                                                                                   | Keeps presentation, Python process, cross-boundary contracts, and scenario data reviewable and independently testable.                                                                                                                                                                                                                     |
| B-002 | Pin Node.js 24.19.0 and pnpm 11.24.0.                                                                                                                                                                                                        | Node 24 is the supported LTS line selected for the workspace; the exact pnpm version makes clean-checkout installs reproducible.                                                                                                                                                                                                           |
| B-003 | Pin the web/tooling baseline to Next.js 16.3.3, React/React DOM 19.2.8, TypeScript 6.0.3, Tailwind CSS and `@tailwindcss/postcss` 4.3.3, PostCSS 8.5.26, Zod 4.4.3, ESLint 9.39.5, and Prettier 3.9.6.                                       | These are the exact stable versions selected from the registry audit for Bootstrap. Package manifests and the lockfile remain the executable source of truth for all transitive versions.                                                                                                                                                  |
| B-004 | Use Python 3.13.7 as the supported local/CI baseline rather than the newest Python release.                                                                                                                                                  | The future Windows Execution Node and MT5 ecosystem benefit from a conservative, explicitly tested interpreter. Bootstrap needs dependable Pydantic, Ruff, mypy, pytest, and wheel support more than a newer language runtime. Moving to a newer Python requires an explicit compatibility review. No MetaTrader package is installed now. |
| B-005 | Pin Worker tooling to Pydantic 2.13.4, pytest 9.1.1, Ruff 0.16.4, mypy 2.3.1, and Hatchling 1.32.0.                                                                                                                                          | Exact pins make local and CI validation reproducible while the Worker is still an inert scaffold.                                                                                                                                                                                                                                          |
| B-006 | The initial scenario is `no_signal`; runtime mode is immutable `SHADOW`.                                                                                                                                                                     | The prototype's `human_approval` default conflicts with the fixed product decision. Simulator scenarios may display future outcomes but never change runtime mode.                                                                                                                                                                         |
| B-007 | Maintain exactly 20 presentation-only scenarios in one authoritative store.                                                                                                                                                                  | Prevents price, tolerance, risk, sample-count, and status drift across components. Scenario switching is a development/test concern, not a command path.                                                                                                                                                                                   |
| B-008 | Use strict runtime validation in both TypeScript (Zod) and Python (Pydantic).                                                                                                                                                                | Compile-time types do not protect JSON/process boundaries. Strict discriminated models reject unknown command types, extra fields, invalid literals, and unsafe numeric values.                                                                                                                                                            |
| B-009 | Keep command contracts but implement no command processor.                                                                                                                                                                                   | Typed envelopes are required for architecture validation. Processing, persistence, claims, retries, approvals, and execution belong to later milestones.                                                                                                                                                                                   |
| B-010 | Treat `live_account_detected` as an invalid/failure visualization, never as supported configuration.                                                                                                                                         | Runtime/proposal configuration accepts Demo only and fails closed when Live is detected. There is no Live feature flag or switch.                                                                                                                                                                                                          |
| B-011 | Keep all Bootstrap UI data explicitly fixture-backed.                                                                                                                                                                                        | Health, MT5, Supabase, LINE, orders, and positions must not be mistaken for real connectivity. No random ticker or timer-driven broker progression is used.                                                                                                                                                                                |
| B-012 | The Development State Simulator and scenario selection are development/test-only; production forces `no_signal`, ignores manual scenario query values, omits scenario propagation from links, and scans the bundle for the simulator marker. | A runtime/public environment switch or query string could accidentally expose presentation states. Production exclusion is a build invariant, not a user preference.                                                                                                                                                                       |
| B-013 | Rebuild production components from tokens and reusable variants.                                                                                                                                                                             | The prototype uses inline styles and its own runtime. Direct conversion would create a monolith and an unauthorized dependency on `support.js` or `_ds`.                                                                                                                                                                                   |
| B-014 | Add only an inert Supabase config, comment-only migration, and functions placeholder.                                                                                                                                                        | Establishes the workflow without inventing untested schema, RLS, browser grants, or secrets. Operational Supabase work starts in Milestone 1.                                                                                                                                                                                              |
| B-015 | Do not implement active approval, pause/resume, emergency, order, or position controls in Bootstrap.                                                                                                                                         | Required states are visual shells. Actions would imply command and broker capabilities that are not authorized.                                                                                                                                                                                                                            |
| B-016 | Defer Storybook, full setup/mobile/risk/journal workflows, P1, and P2.                                                                                                                                                                       | The active prompt requires component tests where practical and only the static Bootstrap surface. Deferral is documented so placeholders are not mistaken for completed P0 behavior.                                                                                                                                                       |
| B-017 | Permit only the lockfile-resolved `unrs-resolver` install script and explicitly list the audited Next.js artifacts in pnpm's minimum-release-age exclusions.                                                                                 | ESLint's resolver requires platform setup. Exact pins, lockfile integrity hashes, pnpm supply-chain validation, and `pnpm audit` remain required; no general dependency-script allowlist is enabled.                                                                                                                                       |

## Fixed assumptions

- The root of the extracted `Aurum_Codex_Starter_Pack` folder is the project root.
- All market/account/order values are fictional design fixtures.
- No real Supabase project, MT5 terminal, LINE channel, or API account is required for installation or tests.
- Local Supabase is optional in Bootstrap because the migration contains no executable schema.
- The `0.01` volume is a maximum. The minimum-lot scenario deliberately has calculated volume 0.0045 and no requested volume.
- A future Worker execution port may describe a boundary but must remain without a broker implementation until explicitly authorized.

## Known Bootstrap limitations

- Static health states do not prove any external system is healthy.
- Contract compatibility is tested only for representative Bootstrap messages, not a live control plane.
- Emergency acknowledgement, approvals, pending/rejected orders, and positions are fixture states.
- No operational RLS, authentication, persistence, reconciliation, or recovery exists yet.
- The exact next authorized task is **Milestone 1 — Supabase Domain and Security Foundation**.

## Milestone 1 decisions

| ID     | Decision                                                                                                                                                                                                                 | Rationale and consequence                                                                                                                                                                                                                                                                                                                                                                                  |
| ------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M1-001 | Run Supabase locally only with CLI `2.115.0`, PostgreSQL 17, Docker, and localhost-bound ports.                                                                                                                          | Milestone 1 needs executable migration, RLS, and database tests but does not authorize a remote project, credentials, linking, deployment, or `db push`.                                                                                                                                                                                                                                                   |
| M1-002 | Put the application tables in the exposed `public` schema, give every owner-scoped record an `owner_id`, enable and force RLS, and use composite ownership foreign keys where a child references an owner-scoped parent. | The initial product has one owner, but cross-user isolation must still be structurally testable. Composite keys prevent a row from linking a resource owned by a different user.                                                                                                                                                                                                                           |
| M1-003 | Separate the authenticated browser user, a dedicated `aurum_worker` NOLOGIN role, and an internal `aurum_function_owner` NOLOGIN role.                                                                                   | Browser and Worker authority must be independently revocable and least-privilege. The Worker design does not use the broad Supabase administrative role. No production JWT or credential is minted or stored in this repository.                                                                                                                                                                           |
| M1-004 | Permit browser writes to protected operational state only through narrowly granted secured functions that create durable intents.                                                                                        | Approval, rejection, pause, resume, Emergency Stop, Position requests, and risk-policy changes become commands; they do not mutate MT5, broker orders, executions, or Positions.                                                                                                                                                                                                                           |
| M1-005 | Keep Supabase Realtime disabled for Milestone 1.                                                                                                                                                                         | Command correctness comes from PostgreSQL transactions, locks, leases, and durable event history. A later notification subscriber may wake a poller, but Realtime is never queue truth.                                                                                                                                                                                                                    |
| M1-006 | Include minimal `market_snapshots` and `feature_snapshots` tables, but no ingestion or feature pipeline.                                                                                                                 | The existing Trade Proposal contract requires immutable snapshot identifiers. The tables preserve referential integrity without starting Milestone 2 or 3 behavior.                                                                                                                                                                                                                                        |
| M1-007 | Store risk policies as an identity row plus immutable version rows and allow secured requests for only the documented bounded numeric rule keys.                                                                         | Previous safety policy remains auditable. Environment, symbol, volume cap, single-position limit, mandatory Stop Loss, and prohibited-strategy booleans are immutable safety invariants, not frontend-editable rule keys.                                                                                                                                                                                  |
| M1-008 | Recover expired leases only before execution; never automatically reclaim an `executing` command.                                                                                                                        | Reclaiming uncertain execution could duplicate a future broker side effect. Pre-execution work can be retried under an incremented attempt count, while execution uncertainty must fail closed and require reconciliation in a later authorized milestone.                                                                                                                                                 |
| M1-009 | Generate local Supabase types into `packages/contracts/src/database.generated.ts`; keep hand-written Zod domain contracts and row-to-domain mappings separate.                                                           | Generated SQL rows contain nullable snake_case persistence fields, while wire/domain models deliberately use validated camelCase and omitted optional fields. Equating the two would hide nullability and precision decisions.                                                                                                                                                                             |
| M1-010 | Create `broker_orders`, `trade_executions`, and `positions` only as protected schema foundations and provide no database function that creates or mutates those records.                                                 | Their presence supports ownership, RLS, and future read models. It does not authorize simulated or real execution, `order_send()`, or MT5 Position modification.                                                                                                                                                                                                                                           |
| M1-011 | Run the pinned local Supabase CLI inside a disposable Docker-in-Docker boundary whose outer ports are verified as `127.0.0.1` only.                                                                                      | Docker Desktop on this Windows host did not honor the direct CLI's intended loopback binding. The guarded wrapper never mounts the host Docker socket, verifies pinned images and labeled resources, suppresses key-bearing CLI output, and removes the isolated stack on every failed check.                                                                                                              |
| M1-012 | Keep the Milestone 1 web integration as an unwired, read-only, owner-scoped adapter boundary.                                                                                                                            | Existing screens remain deterministic fixture views. Persistence rows are mapped into proposal summaries, risk-check lists, Position summaries, component health, and a payload-free command-progress view without exposing protected writes or implying that a proposal summary alone is eligibility evidence.                                                                                            |
| M1-013 | Give a canonical exact idempotent replay precedence over current expiry, target status, and target-version gates; keep changed content on the same key as a conflict.                                                    | A retry identifies the already-recorded intent and must remain deterministic after state changes. Canonical payload validation still occurs first, and an unused key must pass current expiry and resource gates before a new command can be created.                                                                                                                                                      |
| M1-014 | During a Worker claim, deterministically terminalize expired or attempt-exhausted pre-execution commands, and clamp every claim or renewal lease to the command expiry.                                                  | Pending, claimed, or validating work must not remain permanently non-terminal once it is no longer executable. Executing uncertainty remains deliberately untouched, and each safe terminalization records an event plus audit row in the same transaction.                                                                                                                                                |
| M1-015 | Bind every proposal's feature snapshot to the same owner, account, and market snapshot cited by the proposal, and keep `require_calibrated_model` fixed to `false` in this milestone.                                    | The schema may preserve provenance needed by later milestones without accepting contradictory evidence or implying that a calibrated model pipeline exists in Milestone 1.                                                                                                                                                                                                                                 |
| M1-016 | Prove `SKIP LOCKED` behavior with two real, overlapping, passwordless sessions inside the disposable local database rather than using a remote connection from pgTAP.                                                    | A pgTAP transaction cannot observe its own fixture from an independent session, and local passwordless cross-container authentication is deliberately unavailable. The harness verifies and uses the pinned local role graph's existing SET capability without changing membership, rolls back both claims, verifies no event/audit residue, and destroys the isolated volume on any failure.              |
| M1-017 | Return one typed, payload-free result envelope from Worker claim and incident actions, including explicit safe result codes for unauthorized, invalid, no-work, replay, and conflict outcomes.                           | A zero-row or nullable scalar result is ambiguous at a secured boundary. Claim authority remains Worker-only and may return its opaque lease to that Worker, while the browser safe view still omits payload and lease data. Incident idempotency is owner/request scoped: an exact canonical replay returns the original identity, while changed content on the same key fails without mutation or audit. |
| M1-018 | Treat Milestone 1 policy selection as a single-owner, single-account, single-active-policy limitation.                                                                                                                   | The existing request helper selects the one relevant owner policy and does not define deterministic multi-account or multiple-active-policy selection. Milestone 2 may attach sanitized read-only observations to one account binding, but it must not imply or introduce a general multi-account policy architecture.                                                                                     |

## Milestone 1 assumptions and unresolved operational decisions

- The deterministic local Auth owner is fictional, has an `.invalid` email address, and has no seeded password. Tests authenticate by local JWT claims rather than real login information.
- The dedicated Worker authorization tests use fake local claims containing `role=aurum_worker`, `owner_id`, and `worker_id`. Hosted claim issuance, rotation, and revocation are an operational decision for a later deployment task; Milestone 1 creates no remote credential.
- `notifications`, mobile approval sessions, user notes, candles, strategy runs, journals, analytics, and model tables are omitted because no existing Milestone 1 contract requires them. Adding them would start deferred P0/P1 behavior speculatively.
- Numeric database values are authoritative for money, price, percentage, and volume. Generated TypeScript values require deliberate adapters before arithmetic. Milestone 1 Python wire models validate finite numeric inputs but perform no financial arithmetic; a later arithmetic milestone must adopt and test an explicit decimal-conversion policy before calculations.
- Security-relevant successful actions and deterministic domain rejections can be audited in their transaction. A PostgreSQL permission denial rolls back its transaction, so failed authorization attempts belong in platform/authentication logs rather than an impossible same-transaction audit insert.
- Existing 20 development scenarios remain presentation-only fixtures. Executed, order, Position, and Emergency Stop visuals are not inserted as operational seed records and do not represent a functioning execution path.
- Proposal read models expose proposal facts and normalized risk checks separately. They do not infer executable eligibility, and missing or expired heartbeat data maps to `unknown` rather than a healthy state.
- At the Milestone 1 audit, the project root contained local `.git` metadata, but its branch had no commits and no remote. Milestone 1 therefore preserves the deliverable as reviewable working-tree changes and does not fabricate a baseline commit, tag, or push destination.
- After Milestone 1 completion, the user explicitly authorized publication to `Burist106/AITrading`. The existing remote `README.md` commit is preserved, the project is added on top of `main` without force-push, and GitHub Actions verified both clean-checkout jobs on implementation commit `ab7df47`.
- Local queue tests may create fictional Worker claims and operational rows inside rolled-back test transactions. They are evidence for database behavior only and do not represent a deployed Worker, broker session, order, execution, or Position mutation.
- The two-session queue test assumes local peer access and the existing `postgres` to `aurum_worker` SET capability inside the pinned Supabase database container. It never reads or requests a database password and never changes role membership; cleanup verifies the baseline capability remains present, while any failed path destroys the isolated database volume.
- The current deployment model is explicitly one owner, one Demo account binding, and one active risk policy. Multiple accounts, broker sessions, or active policy selection are unresolved future architecture and are not inferred by Milestone 2.

## Milestone 2 decisions

| ID     | Decision                                                                                                                                                                                                              | Rationale and consequence                                                                                                                                                                                                                                                                                                                                                                          |
| ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| M2-001 | Pin the official optional boundary to `MetaTrader5==5.0.6090`, Python `3.13.7`, Windows only.                                                                                                                         | Linux quality gates stay package-independent. A bounded Windows CI job proves exact package resolution, import, native-wrapper tests, and scanner behavior without a Terminal or credentials.                                                                                                                                                                                                      |
| M2-002 | Use one statically visible `MetaTrader5ReadAdapter` and serialize every native call with a process-wide re-entrant lock and one active owner.                                                                         | The native module has process-global connection state. Static direct calls make allowlist review and AST enforcement possible; dynamic dispatch is forbidden.                                                                                                                                                                                                                                      |
| M2-003 | Pass only an explicit local executable path as the sole positional argument to `initialize()` and never accept an MT5 password.                                                                                       | The already-open terminal session is the only identity source. `account_info()` values are transiently masked/fingerprinted, and non-Demo, disconnected, or binding-mismatch state fails closed before any later read.                                                                                                                                                                             |
| M2-004 | Never auto-bind a discovered or observed symbol, and require reported base `XAU` plus profit `USD` for canonical `XAUUSD`.                                                                                            | A broker name or description is not currency evidence. Configuration selects a candidate only; an immutable owner-scoped confirmation selects the binding. Ambiguity, invisibility, missing fields, absent confirmation, or changed specification blocks healthy state.                                                                                                                            |
| M2-005 | Convert native numerics with `Decimal(str(value))`, serialize decimals as canonical strings, and serialize tickets as strings.                                                                                        | This avoids binary-float arithmetic at Worker boundaries and JavaScript safe-integer loss. NaN, infinity, malformed strings, and invalid price/volume constraints are rejected.                                                                                                                                                                                                                    |
| M2-006 | Use UTC-aware bucket boundaries, exclude incomplete current candles by default, cap candle count at 2,000, and cap history windows at 168 hours.                                                                      | Completeness is `open_at + timeframe_duration <= now`, never “last row means current.” Native time is normalized inside safe failure mapping; incomplete bars and unbounded history cannot silently enter downstream state. Gap metadata remains explicit.                                                                                                                                         |
| M2-007 | Reconcile before reporting healthy after startup or reconnect.                                                                                                                                                        | The Worker compares account/server/confirmed-specification bindings, open Position and active Order ticket sets, executing-command uncertainty, current bounded history evidence, and clock/tick state. A mismatch records evidence and blocks; it never creates execution or mutates state.                                                                                                       |
| M2-008 | Add six owner-scoped MT5 tables and seven least-privilege Worker RPCs rather than reuse future market/feature tables.                                                                                                 | Raw ticks do not fabricate ATR, regime, or session classifications. Separate per-run Order/Deal evidence, forced RLS, owner-derived claims, exact JSON keys, and function-owner policies preserve the Milestone 1 authority model. Routine tick telemetry does not grow security audit logs.                                                                                                       |
| M2-009 | Keep the P0 Web Console fixture-driven while adding an owner-scoped MT5 read adapter and bounded observation panel.                                                                                                   | The UI can represent loading, empty, degraded, blocked, stale, reconnecting, reconciliation-pending, and reconciliation-failed states without adding a broker control or implying a deployed live connection.                                                                                                                                                                                      |
| M2-010 | Make the real-terminal smoke command opt-in and authoritative after it starts.                                                                                                                                        | CI never has a broker session and never runs the smoke. Flag/path preconditions may produce `NOT RUN` with exit 0 before startup; an opted-in safety block exits 2, a technical failure exits 3, and only a fully checked run plus clean shutdown may report `PASSED` with exit 0. A smoke-only confirmed fingerprint is supplied before the run and is never derived from that run's observation. |
| M2-011 | Keep observed specification evidence permanently separate from explicit immutable confirmation versions.                                                                                                              | Recording a changed observation cannot make it the next comparison baseline. Confirmed A plus observed B remains blocked across cycles until a future-authorized/manual setup writes a new confirmed B version. No production confirmation UI or RPC is introduced here.                                                                                                                           |
| M2-012 | Persist exactly one Order and one Deal history-query evidence record for each completed full reconciliation.                                                                                                          | Empty tuples are valid empty results; native `None` is failure. Exact request boundaries, completion time, counts, returned boundaries, state, and safe reason prevent a stale or hard-coded completeness claim.                                                                                                                                                                                   |
| M2-013 | Separate tick, Position/active-Order, and full-reconciliation cadences.                                                                                                                                               | Five-second-class market polling must not query history or create reconciliation/audit records. Startup, reconnect, scheduled full cycles, and explicit inconsistencies obtain current history evidence; conservative defaults avoid broker/database load.                                                                                                                                         |
| M2-014 | Treat every safety-sensitive native boolean as strict evidence.                                                                                                                                                       | Missing or malformed `connected`, `visible`, and related fields cannot be coerced into a safe truth value. Terminal connection validation occurs before account verification, so uncertain native state fails closed.                                                                                                                                                                              |
| M2-015 | Bound lightweight tick polling to 1–30 seconds and expire the Web liveness projection after 60 seconds without a fresh tick observation.                                                                              | Two missed maximum-length polls remove the browser's connected/available claim. A matched reconciliation is displayed as healthy only while its account, server, broker symbol, and specification fingerprints still match the latest owner-scoped observations.                                                                                                                                   |
| M2-016 | Validate native currency codes after whitespace trimming but never normalize or rewrite their case.                                                                                                                   | Canonical XAU/USD evidence must arrive exactly as base `XAU` and profit `USD`; lower-case or otherwise altered broker values fail closed instead of being transformed into acceptable evidence. Margin currency remains policy-neutral but must still satisfy the exact uppercase currency-code contract.                                                                                          |
| M2-017 | Continue bounded lightweight polling while a connected read-capable state is degraded or blocked, and edge-trigger full reconciliation.                                                                               | Current tick liveness and Position/active-Order changes remain observable without querying history every short cycle. A stable non-live or otherwise blocked condition does not flood full reconciliations, while a material change or recovery requires a new full cycle before health can return to Healthy.                                                                                     |
| M2-018 | Serialize reconciliation mismatch append and completion on the owner-scoped parent run and require an exact child/payload mismatch set.                                                                               | A run cannot complete as `matched` after persisted mismatch evidence was added, and no new mismatch may be appended after completion. Exact normalized-set comparison prevents same-count substitution while preserving idempotent replay of already-recorded evidence.                                                                                                                            |
| M2-019 | Give the long-running poller sole ownership of three typed component heartbeats: `execution.worker`, `execution.mt5_adapter`, and `execution.market_data`.                                                            | One publisher derives all three states from the same local authoritative state, preventing reconciliation and short polling from emitting contradictory truths. Full and tick cycles renew all three; Position/active-Order cycles renew Worker and adapter only. The one-shot smoke CLI does not claim long-running liveness.                                                                     |
| M2-020 | Default heartbeat TTL to 30 seconds, bound it to 15–300 seconds, and require it to be at least three configured tick intervals.                                                                                       | With default tick/Position/full cadences of 5/15/600 seconds, routine tick polling renews liveness several times before expiry while configuration fails closed when the producer cannot satisfy its TTL. The seeded expected interval remains 15 seconds for each enabled execution component.                                                                                                    |
| M2-021 | Cap `execution.worker` by the last authoritative full reconciliation and make `reconciliation_required` incompatible with Healthy.                                                                                    | Lightweight reads may renew or lower the Worker state but cannot promote it. A fresh tick may make only `execution.market_data` Healthy; it cannot erase a blocked/degraded Worker result, clear reconciliation, or establish system/trading eligibility.                                                                                                                                          |
| M2-022 | Map tick freshness consistently as `LIVE → healthy/HEALTHY`, `DELAYED → degraded/TICK_DELAYED`, `STALE → failed/TICK_STALE`, `FUTURE_INVALID → failed/TICK_FROM_FUTURE`, and `UNAVAILABLE → failed/TICK_UNAVAILABLE`. | Delayed data is observably different from stale data. The mapping is shared by full and lightweight paths and gives `execution.market_data` a narrow component meaning without weakening top-level eligibility.                                                                                                                                                                                    |
| M2-023 | Persist routine ticks and component heartbeats as bounded versioned upserts without a per-update security-audit append.                                                                                               | Operational telemetry must not flood `audit_logs`. Important lifecycle incidents and security-sensitive command, authorization, and configuration actions remain audited. The heartbeat change is an additive replacement of the existing RPC implementation, preserving its signature, forced RLS, owner isolation, and grants.                                                                   |
| M2-024 | Derive Web heartbeat `unknown` from missing, expired, duplicate, or invalid evidence rather than allowing producers to report it.                                                                                     | Only `healthy`, `degraded`, and `failed` cross the producer boundary. Invalid rows expose no unsafe detail, and a component Healthy state remains a narrow read-only diagnostic rather than proof of strategy, risk, approval, command, or trading eligibility.                                                                                                                                    |

## Milestone 2 assumptions and unresolved limitations

- The deployment remains single-owner, single-Demo-account binding, single confirmed broker symbol, and single Worker process. Multi-account selection and distributed native ownership are not designed.
- The local Terminal installation, Demo session, Market Watch visibility, and broker-specific XAUUSD symbol are operator prerequisites. The Worker does not install a Terminal, enable a symbol, or sign in.
- Latest tick persistence is intentionally bounded to one versioned current row per owner/account/symbol. Candle and raw tick history persistence are deferred.
- The P0 browser adapter derives a safe health projection from available observation rows; terminal version and last completed candle remain nullable when no persisted source exists.
- The Web Console derives component liveness from one bounded heartbeat row per enabled execution component. A current tick remains separate market evidence; neither a fresh tick nor a healthy component proves trading eligibility.
- The implementation assumes one long-running poller owns component heartbeat publication for the single Worker process. Multiple publishers, distributed leases, and leader election remain outside this milestone.
- A completed authoritative full cycle may clear the `reconciliation_required` scheduling gate even when its result is Degraded or Blocked. That prevents immediate full-cycle flooding; the persisted Worker heartbeat remains Degraded or Failed, and only a later Healthy full cycle can promote it to Healthy.
- Heartbeat persistence failure is not retried recursively. The original safe failure remains authoritative, local state stays failed/unavailable, and ordinary polling/reconnect policy decides the next bounded attempt.
- The heartbeat/liveness final local gates passed on 2026-09-02: 88 TypeScript tests, 233 Worker tests, 400 pgTAP assertions, four concurrent-claim assertions, builds, generated types, dependency checks, and security scans. Pull Request run `33541088560` then passed `quality`, `database`, and `windows-mt5-boundary` on implementation commit `3e25007`; earlier source-review CI is not reused as evidence.
- Hosted Worker claim issuance, secret rotation, deployment, monitoring, and remote Supabase configuration remain unresolved and were not performed.
- Changed-specification confirmation remains an operationally manual or future-authorized setup operation. This patch intentionally exposes no browser or Worker confirmation action.
- The source-review patch assumes one bounded 24-hour history request can prove only that exact request completed; it makes no claim about unlimited broker retention outside the requested window.
- The real-terminal smoke is `NOT RUN` because no explicitly opted-in eligible local Demo Terminal configuration was supplied. Deterministic tests and Windows import checks are not a substitute for that smoke.
- At the Milestone 2 release, Milestone 3 and all execution capabilities were unstarted. The separately authorized 2026-09-22 production implementation is recorded above; command consumption and broker execution remain unauthorized.
